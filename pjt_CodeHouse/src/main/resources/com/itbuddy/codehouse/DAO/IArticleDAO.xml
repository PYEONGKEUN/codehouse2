<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itbuddy.codehouse.DAO.IArticleDAO">
	<select id="selectArticleListOrderByParmDesc" resultType="com.itbuddy.codehouse.DTO.Article">
		SELECT *
		FROM article
		ORDER BY #{param}
		LIMIT #{start},#{count}
	</select>
</mapper>
